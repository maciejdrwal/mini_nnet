# Mini Neural Net

Neural networks implementation in C++. Includes autograd.

## Building

Make sure [CMake](http://cmake.org) is installed and available in PATH. To build please use:

`rm -rf build ;  mkdir build ; cd build ; cmake .. ; cmake --build .`
